<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd">



    <!-- ################# 操作Redis集群版(分布式缓存) ############### -->
    <!-- 配置资源属性源 -->
    <!--<bean id="propertySource" class="org.springframework.core.io.support.ResourcePropertySource">
        <constructor-arg name="resource" value="classpath:props/redis-config.properties"/>
    </bean>

    &lt;!&ndash; 配置Redis集群节点信息 &ndash;&gt;
    <bean id="clusterConfig" class="org.springframework.data.redis.connection.RedisClusterConfiguration">
        &lt;!&ndash; 配置属性源(指定节点信息) &ndash;&gt;
        <constructor-arg name="propertySource" ref="propertySource"/>
    </bean>-->
    <!-- 配置连接工厂 -->
   <!-- <bean id="connectionFactory" class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory">
        <constructor-arg name="clusterConfig" ref="clusterConfig"/>
    </bean>-->

    <!-- 配置RedisTemplate操作Redis -->
    <bean id="redisTemplate" class="org.springframework.data.redis.core.RedisTemplate">
        <!-- 设置连接工厂 -->
        <property name="connectionFactory" ref="connectionFactory"/>
    </bean>
    <!-- ################# 操作Redis单机版 ############### -->
    <!-- 配置连接工厂 -->

    <bean id="connectionFactory" class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory">
        <!-- 设置host -->
        <property name="hostName" value="192.168.12.131"/>
        <!-- 设置port -->
        <property name="port" value="6379"/>
    </bean>
</beans>